# Unipile Dev Assistant

## Backend
- Python 3.12
- FastAPI
- MongoDB
- Run: `uvicorn Backend.api:app --reload`

## Frontend
- React / Next.js
- Run: `npm install` then `npm run dev` inside `Frontend/`

## Notes
- Backend `data/` folder contains ingestion text files.
